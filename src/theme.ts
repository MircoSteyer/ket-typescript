import {createMuiTheme, Theme} from "@material-ui/core";

export const theme: Theme = createMuiTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#002642',
        },
        secondary: {
            main: '#92140c',
        },
    }
})
